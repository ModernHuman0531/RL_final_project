# --------------------------------
# For linux users(Windows users should # out the following lines)
DISPLAY=:1
# Linux needs to mount the X11 socket and set the DISPLAY variable
GUI_MOUNT=-v /tmp/.X11-unix:/tmp/.X11-unix

# -------------------------------



# --------------------------------
# For windows users(Linux users should # out the following lines)
# DISPLAY=host.docker.internal:0
# GUI_MOUNT=
# -------------------------------